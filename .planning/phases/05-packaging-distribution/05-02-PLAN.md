---
phase: 05-packaging-distribution
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - PKG-04

must_haves:
  truths:
    - "VS Code integration tests run on every push and PR via ci.yml"
    - "Linux runner uses xvfb-run -a npm test for virtual display"
    - "macOS runner uses npm test directly (no Xvfb needed)"
    - "Integration tests run after build step and before manifest verify"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Xvfb integration test steps on both platforms"
      contains: "xvfb-run -a npm test"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "extension/package.json test script"
      via: "npm test → vscode-test"
      pattern: "xvfb-run.*npm test"
---

<objective>
Add VS Code integration tests (Xvfb on Linux, direct on macOS) to the existing ci.yml workflow.

Purpose: Per D-11, integration tests must run on every push/PR alongside typecheck and build. This replaces the Phase 1 deferral comment at lines 69-73 of ci.yml. Integration tests also gate the release workflow (D-12) — they must be proven working in CI before the release workflow references the same pattern.

Output: Updated ci.yml with conditional Xvfb test steps.
</objective>

<execution_context>
@/Users/spencersr/src/github/whardier/which-code/.claude/get-shit-done/workflows/execute-plan.md
@/Users/spencersr/src/github/whardier/which-code/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-packaging-distribution/05-CONTEXT.md
@.planning/phases/05-packaging-distribution/05-RESEARCH.md
@.planning/phases/05-packaging-distribution/05-PATTERNS.md

<interfaces>
<!-- Current ci.yml structure for executor reference -->

From .github/workflows/ci.yml (full current content):
```yaml
name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    name: Build and typecheck (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [macos-latest, ubuntu-latest]
      fail-fast: false

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: extension/package-lock.json

      - name: Install dependencies
        working-directory: extension
        run: npm ci

      - name: TypeScript typecheck
        working-directory: extension
        run: npx tsc --noEmit

      - name: Build extension bundle
        working-directory: extension
        run: npm run build

      - name: Verify manifest
        working-directory: extension
        run: |
          node -e "..."

      - name: Verify dist output
        working-directory: extension
        run: test -f dist/extension.js && echo "dist/extension.js exists"

      - name: Check no save trigger
        working-directory: extension
        run: |
          if grep -r "onDidSaveTextDocument" src/ --exclude-dir=test; then ...

# Note: Full integration tests (npm run test via @vscode/test-electron) require
# Xvfb on Linux (virtual framebuffer) and a real GUI session. That setup is
# deferred to Phase 4 (Marketplace packaging). Phase 1 CI validates:
# typecheck, build, manifest checks, and static source assertions.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Xvfb integration test steps to ci.yml</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (full file — must see exact line numbers for insertion point and the deferred comment to remove)
    - .planning/phases/05-packaging-distribution/05-RESEARCH.md (Pattern 2: Xvfb Integration Tests on Linux CI)
    - .planning/phases/05-packaging-distribution/05-PATTERNS.md (ci.yml pattern assignment section)
  </read_first>
  <action>
Modify `.github/workflows/ci.yml` with exactly two changes:

**Change 1 — Insert two conditional test steps after the "Build extension bundle" step (after line 39) and before "Verify manifest" (line 41):**

Insert these two steps between "Build extension bundle" and "Verify manifest":

```yaml
      - name: Run integration tests (Linux)
        if: runner.os == 'Linux'
        working-directory: extension
        run: xvfb-run -a npm test

      - name: Run integration tests (macOS)
        if: runner.os != 'Linux'
        working-directory: extension
        run: npm test
```

**Exact insertion point:** After the step with `run: npm run build` (currently line 39) and before the step with `name: Verify manifest` (currently line 41). The two new steps run between build and manifest verification.

**Why two conditional steps instead of one:** Linux CI runners have no display server — `xvfb-run -a` creates a virtual framebuffer. macOS runners have a GUI session by default. The `runner.os == 'Linux'` condition correctly routes to the right command. This matches the official VS Code CI documentation pattern (RESEARCH.md Pattern 2).

**Change 2 — Remove the deferred comment block at the end of the file (lines 69-73):**

Delete these lines completely:
```yaml
# Note: Full integration tests (npm run test via @vscode/test-electron) require
# Xvfb on Linux (virtual framebuffer) and a real GUI session. That setup is
# deferred to Phase 4 (Marketplace packaging). Phase 1 CI validates:
# typecheck, build, manifest checks, and static source assertions.
```

This comment is now obsolete — integration tests are no longer deferred.

**No other changes.** Preserve all existing steps (Checkout, Setup Node.js, Install dependencies, TypeScript typecheck, Build, Verify manifest, Verify dist output, Check no save trigger).
  </action>
  <verify>
    <automated>cd /Users/spencersr/src/github/whardier/which-code && grep -c "xvfb-run" .github/workflows/ci.yml && grep -c "npm test" .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - .github/workflows/ci.yml contains `xvfb-run -a npm test`
    - .github/workflows/ci.yml contains `if: runner.os == 'Linux'`
    - .github/workflows/ci.yml contains `if: runner.os != 'Linux'`
    - .github/workflows/ci.yml contains `name: Run integration tests (Linux)` BEFORE `name: Verify manifest`
    - .github/workflows/ci.yml does NOT contain `deferred to Phase 4` (comment removed)
    - .github/workflows/ci.yml does NOT contain `deferred to Phase` (no deferral comments)
    - Existing steps (Checkout, Setup Node.js, Install dependencies, TypeScript typecheck, Build extension bundle, Verify manifest, Verify dist output, Check no save trigger) are all still present
    - Matrix is unchanged: `os: [macos-latest, ubuntu-latest]` with `fail-fast: false`
  </acceptance_criteria>
  <done>ci.yml has conditional Xvfb integration test steps after build and before manifest verify, deferral comment removed, all existing steps preserved</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner -> test framework | npm test invokes @vscode/test-cli which downloads VS Code binary and runs tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-CI-01 | Tampering | xvfb-run on CI | accept | xvfb-run is a standard system utility on ubuntu-latest; installed by default; no user-controlled input |
</threat_model>

<verification>
1. `grep "xvfb-run" .github/workflows/ci.yml` — Xvfb step present
2. `grep "runner.os" .github/workflows/ci.yml` — conditional logic present
3. `grep -c "deferred" .github/workflows/ci.yml` — returns 0 (comment removed)
4. YAML syntax check: `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"` (or `yq` if available)
</verification>

<success_criteria>
- ci.yml contains two conditional test steps (Linux with Xvfb, macOS direct)
- Test steps appear between Build and Verify manifest
- Phase 1 deferral comment is removed
- All existing CI steps are preserved unchanged
- YAML is syntactically valid
</success_criteria>

<output>
After completion, create `.planning/phases/05-packaging-distribution/05-02-SUMMARY.md`
</output>
