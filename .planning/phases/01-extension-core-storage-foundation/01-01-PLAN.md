---
phase: 01-extension-core-storage-foundation
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - extension/package.json
  - extension/tsconfig.json
  - extension/esbuild.js
  - extension/.vscodeignore
  - extension/.gitignore
  - extension/.vscode-test.js
  - extension/src/extension.ts
  - extension/src/db.ts
  - extension/src/session.ts
  - extension/src/storage.ts
  - extension/src/config.ts
  - extension/src/test/extension.test.ts
autonomous: true
requirements:
  - EXT-01
  - EXT-02
  - EXT-03
  - EXT-04
  - EXT-05
  - PLAT-01

must_haves:
  truths:
    - "extension/package.json exists with publisher 'whardier', name 'this-code', extensionKind ['workspace'], activationEvents ['onStartupFinished'], no contributes.commands, exactly two settings"
    - "extension/tsconfig.json exists with strict: true and noEmit: true"
    - "extension/esbuild.js marks '@vscode/sqlite3' and 'vscode' as external"
    - "extension/.vscodeignore contains '!node_modules/@vscode/sqlite3/**'"
    - "extension/src/extension.ts exports activate() and deactivate()"
    - "extension/src/db.ts exports Database class and initDatabase function"
    - "extension/src/session.ts exports SessionMetadata interface, collectSessionMetadata, getSessionJsonPath"
    - "extension/src/storage.ts exports writeSessionJson and scanExistingRemoteSessions"
    - "extension/src/config.ts exports isEnabled and getLogLevel"
    - "npm install completes; node_modules/@vscode/sqlite3 exists"
    - "Integration test stubs exist for all 16 Phase 1 requirements"
  artifacts:
    - path: "extension/package.json"
      provides: "Extension manifest with all contributes fields"
      contains: "whardier.this-code"
    - path: "extension/tsconfig.json"
      provides: "TypeScript strict configuration"
      contains: '"strict": true'
    - path: "extension/esbuild.js"
      provides: "Build script with native module exclusion"
      contains: "@vscode/sqlite3"
    - path: "extension/.vscodeignore"
      provides: "VSIX packaging exclusions with sqlite3 preserved"
      contains: "!node_modules/@vscode/sqlite3"
    - path: "extension/.vscode-test.js"
      provides: "Test runner configuration for @vscode/test-cli"
    - path: "extension/src/extension.ts"
      provides: "activate() and deactivate() entrypoint stubs"
      exports: ["activate", "deactivate"]
    - path: "extension/src/db.ts"
      provides: "@vscode/sqlite3 Promise wrapper + initDatabase"
      exports: ["Database", "initDatabase"]
    - path: "extension/src/session.ts"
      provides: "SessionMetadata type + collection functions"
      exports:
        ["SessionMetadata", "collectSessionMetadata", "getSessionJsonPath"]
    - path: "extension/src/storage.ts"
      provides: "JSON file write and startup scan"
      exports: ["writeSessionJson", "scanExistingRemoteSessions"]
    - path: "extension/src/config.ts"
      provides: "Configuration reading helpers"
      exports: ["isEnabled", "getLogLevel", "LogLevel"]
    - path: "extension/src/test/extension.test.ts"
      provides: "Integration test stubs for all 16 requirements"
  key_links:
    - from: "extension/esbuild.js"
      to: "extension/src/extension.ts"
      via: "entryPoints config"
      pattern: "entryPoints.*extension.ts"
    - from: "extension/src/extension.ts"
      to: "extension/src/db.ts"
      via: "import"
      pattern: "from './db'"
    - from: "extension/package.json"
      to: "extension/dist/extension.js"
      via: "main field"
      pattern: '"main".*dist/extension.js'
---

<objective>
Scaffold the extension/ subdirectory from scratch with all project configuration files, TypeScript source stubs defining all module interfaces, test infrastructure, and npm dependencies installed. This is Wave 0 — every subsequent plan depends on the contracts exported by these stubs.

Purpose: Downstream plans (Waves 1–3) need (1) a compilable build system so type-checking works during implementation, (2) interface contracts (SessionMetadata, Database class) so executors know exactly what to implement, and (3) test stubs with automated verify commands for the Nyquist requirement.

Output: extension/ directory with package.json, tsconfig.json, esbuild.js, .vscodeignore, .vscode-test.js, five TypeScript source stubs, test stubs, and npm install complete.
</objective>

<execution_context>
@/Users/spencersr/src/github/whardier/this-code/.claude/get-shit-done/workflows/execute-plan.md
@/Users/spencersr/src/github/whardier/this-code/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/spencersr/src/github/whardier/this-code/.planning/PROJECT.md
@/Users/spencersr/src/github/whardier/this-code/.planning/ROADMAP.md
@/Users/spencersr/src/github/whardier/this-code/.planning/phases/01-extension-core-storage-foundation/01-CONTEXT.md
@/Users/spencersr/src/github/whardier/this-code/.planning/phases/01-extension-core-storage-foundation/01-RESEARCH.md
@/Users/spencersr/src/github/whardier/this-code/.planning/phases/01-extension-core-storage-foundation/01-VALIDATION.md

<interfaces>
<!-- Contracts THIS plan creates that Plans 02-07 implement against. -->

extension/src/db.ts will export:
class Database {
constructor(dbPath: string)
run(sql: string, params?: unknown[]): Promise<sqlite3.RunResult>
get<T>(sql: string, params?: unknown[]): Promise<T | undefined>
all<T>(sql: string, params?: unknown[]): Promise<T[]>
close(): Promise<void>
}
async function initDatabase(db: Database): Promise<void>

extension/src/session.ts will export:
interface SessionMetadata {
workspace_path: string | null
user_data_dir: string | null
profile: string | null
local_ide_path: string
remote_name: string | null
remote_server_path: string | null
server_commit_hash: string | null
local_session_hash: string
}
function collectSessionMetadata(context: vscode.ExtensionContext): SessionMetadata
function getSessionJsonPath(metadata: SessionMetadata): string

extension/src/storage.ts will export:
function writeSessionJson(filePath: string, metadata: SessionMetadata): Promise<void>
function scanExistingRemoteSessions(db: Database, binDir?: string): Promise<void>

extension/src/config.ts will export:
function isEnabled(): boolean
type LogLevel = 'off' | 'info' | 'debug'
function getLogLevel(): LogLevel
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write six configuration files (package.json, tsconfig.json, esbuild.js, .vscodeignore, .gitignore, .vscode-test.js) and run npm install</name>
  <files>
    extension/package.json
    extension/tsconfig.json
    extension/esbuild.js
    extension/.vscodeignore
    extension/.gitignore
    extension/.vscode-test.js
  </files>
  <read_first>
    .planning/phases/01-extension-core-storage-foundation/01-CONTEXT.md
    .planning/phases/01-extension-core-storage-foundation/01-RESEARCH.md
  </read_first>
  <action>
Create the `extension/` directory at the project root. All six files go inside extension/ — NOT at the project root (root package.json contains only GSD and must not be modified).

**extension/package.json** (complete manifest, locked per EXT-01 through EXT-05, D-03):

```json
{
  "name": "this-code",
  "displayName": "This Code",
  "description": "Session tracking for VS Code launch context and open file manifests",
  "version": "0.1.0",
  "publisher": "whardier",
  "engines": { "vscode": "^1.75.0" },
  "categories": ["Other"],
  "activationEvents": ["onStartupFinished"],
  "main": "./dist/extension.js",
  "extensionKind": ["workspace"],
  "contributes": {
    "configuration": {
      "title": "This Code",
      "properties": {
        "thisCode.enable": {
          "type": "boolean",
          "default": true,
          "description": "Enable session recording."
        },
        "thisCode.logLevel": {
          "type": "string",
          "enum": ["off", "info", "debug"],
          "default": "info",
          "description": "Output channel verbosity."
        }
      }
    }
  },
  "scripts": {
    "build": "node esbuild.js",
    "build:prod": "node esbuild.js --production",
    "watch": "node esbuild.js --watch",
    "typecheck": "tsc --noEmit",
    "test": "vscode-test",
    "package": "vsce package"
  },
  "dependencies": {
    "@vscode/sqlite3": "^5.1.12-vscode"
  },
  "devDependencies": {
    "@types/vscode": "^1.75.0",
    "typescript": "~5.7.0",
    "esbuild": "^0.28.0",
    "@vscode/vsce": "^3.9.0",
    "@vscode/test-cli": "^0.0.12",
    "@vscode/test-electron": "^2.5.2"
  }
}
```

Validation requirements: publisher MUST be `whardier`, name MUST be `this-code`, extensionKind MUST be `["workspace"]`, activationEvents MUST include `onStartupFinished`, `contributes.commands` MUST NOT exist, exactly two configuration properties: `thisCode.enable` and `thisCode.logLevel`. Do NOT add `thisCode.dbPath` (deferred per CONTEXT.md).

**extension/tsconfig.json** (TypeScript strict mode, noEmit — esbuild does transpilation):

```json
{
  "compilerOptions": {
    "target": "ES2020",
    "module": "commonjs",
    "lib": ["ES2020"],
    "outDir": "./out",
    "rootDir": "./src",
    "strict": true,
    "noEmit": true,
    "esModuleInterop": true,
    "forceConsistentCasingInFileNames": true,
    "skipLibCheck": true,
    "sourceMap": true
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules", "dist"]
}
```

**extension/esbuild.js** (bundler; BOTH vscode and @vscode/sqlite3 must be external):

```javascript
const esbuild = require("esbuild");

const production = process.argv.includes("--production");
const watch = process.argv.includes("--watch");

async function main() {
  const ctx = await esbuild.context({
    entryPoints: ["src/extension.ts"],
    bundle: true,
    format: "cjs",
    minify: production,
    sourcemap: !production,
    sourcesContent: false,
    platform: "node",
    outfile: "dist/extension.js",
    external: ["vscode", "@vscode/sqlite3"],
    logLevel: "warning",
  });
  if (watch) {
    await ctx.watch();
  } else {
    await ctx.rebuild();
    await ctx.dispose();
  }
}
main().catch((e) => {
  console.error(e);
  process.exit(1);
});
```

CRITICAL: `external: ['vscode', '@vscode/sqlite3']` — @vscode/sqlite3 has a native `.node` binary that esbuild cannot bundle. Omitting it from external causes activation failure on VSIX install.

**extension/.vscodeignore** (CRITICAL: preserve @vscode/sqlite3 native binary in VSIX):

```
.vscode/**
.vscode-test/**
src/**
out/**
node_modules/**
!node_modules/@vscode/sqlite3/**
*.ts
tsconfig.json
esbuild.js
.gitignore
```

The line `!node_modules/@vscode/sqlite3/**` MUST be present after the `node_modules/**` exclusion. Without it the native binary is excluded from the VSIX.

**extension/.gitignore**:

```
node_modules/
dist/
out/
*.vsix
.vscode-test/
```

**extension/.vscode-test.js** (test runner config for @vscode/test-cli — no index.ts needed; @vscode/test-cli discovers tests via glob automatically):

```javascript
const { defineConfig } = require("@vscode/test-cli");

module.exports = defineConfig({
  files: "src/test/**/*.test.js",
  workspaceFolder: ".",
  mocha: {
    timeout: 60000,
  },
});
```

After writing all six files, run npm install from the extension directory:

```bash
cd /Users/spencersr/src/github/whardier/this-code/extension && npm install
```

This installs @vscode/sqlite3 (with prebuilt native binary for the current platform), typescript, esbuild, @vscode/vsce, @vscode/test-cli, and @vscode/test-electron.
</action>
<verify>
<automated>cd /Users/spencersr/src/github/whardier/this-code && node -e "const p=require('./extension/package.json');const id=p.publisher+'.'+p.name;if(id!=='whardier.this-code')throw new Error('Wrong ID: '+id);if(JSON.stringify(p.extensionKind)!=='[\"workspace\"]')throw new Error('extensionKind wrong');if(!p.activationEvents.includes('onStartupFinished'))throw new Error('activationEvents wrong');if(p.contributes.commands)throw new Error('commands must not exist');const keys=Object.keys(p.contributes.configuration.properties).sort().join(',');if(keys!=='thisCode.enable,thisCode.logLevel')throw new Error('settings wrong: '+keys);console.log('manifest OK: '+id)" && test -d /Users/spencersr/src/github/whardier/this-code/extension/node_modules/@vscode/sqlite3 && echo "@vscode/sqlite3 installed" && grep -q "!node_modules/@vscode/sqlite3" /Users/spencersr/src/github/whardier/this-code/extension/.vscodeignore && echo ".vscodeignore OK"</automated>
</verify>
<done> - extension/package.json passes full manifest validation (ID, extensionKind, activationEvents, no commands, two settings) - extension/node_modules/@vscode/sqlite3/ exists with native binary - extension/.vscodeignore contains the sqlite3 preservation line - extension/tsconfig.json has "strict": true and "noEmit": true - extension/esbuild.js has both 'vscode' and '@vscode/sqlite3' in external array
</done>
</task>

<task type="auto">
  <name>Task 2: Create five TypeScript source stubs (extension.ts, db.ts, session.ts, storage.ts, config.ts) and integration test stubs</name>
  <files>
    extension/src/extension.ts
    extension/src/db.ts
    extension/src/session.ts
    extension/src/storage.ts
    extension/src/config.ts
    extension/src/test/extension.test.ts
  </files>
  <read_first>
    extension/package.json
    .planning/phases/01-extension-core-storage-foundation/01-RESEARCH.md
    .planning/phases/01-extension-core-storage-foundation/01-VALIDATION.md
  </read_first>
  <action>
Create extension/src/ and extension/src/test/ directories, then write all six TypeScript source files. These are stubs — they export the correct interfaces and types but contain minimal/placeholder implementations. Plans 02-06 fill in the implementations.

Do NOT create extension/src/test/index.ts — @vscode/test-cli discovers tests via the glob in .vscode-test.js automatically. A manual Mocha runner index.ts is not needed and would conflict with @vscode/test-cli's runner contract.

**extension/src/db.ts** — @vscode/sqlite3 Promise wrapper (complete implementation; this is the full ~20-line wrapper used by all other modules):

```typescript
import sqlite3 from "@vscode/sqlite3";

export class Database {
  private db: sqlite3.Database;

  constructor(dbPath: string) {
    this.db = new sqlite3.Database(dbPath, (err) => {
      if (err) {
        throw err;
      }
    });
  }

  run(sql: string, params: unknown[] = []): Promise<sqlite3.RunResult> {
    return new Promise((resolve, reject) => {
      this.db.run(
        sql,
        params,
        function (this: sqlite3.RunResult, err: Error | null) {
          if (err) {
            reject(err);
          } else {
            resolve(this);
          }
        },
      );
    });
  }

  get<T>(sql: string, params: unknown[] = []): Promise<T | undefined> {
    return new Promise((resolve, reject) => {
      this.db.get(sql, params, (err: Error | null, row: T) => {
        if (err) {
          reject(err);
        } else {
          resolve(row);
        }
      });
    });
  }

  all<T>(sql: string, params: unknown[] = []): Promise<T[]> {
    return new Promise((resolve, reject) => {
      this.db.all(sql, params, (err: Error | null, rows: T[]) => {
        if (err) {
          reject(err);
        } else {
          resolve(rows as T[]);
        }
      });
    });
  }

  close(): Promise<void> {
    return new Promise((resolve, reject) => {
      this.db.close((err) => {
        if (err) {
          reject(err);
        } else {
          resolve();
        }
      });
    });
  }
}

export async function initDatabase(_db: Database): Promise<void> {
  // Implemented in Plan 02 (Wave 1)
  // Will run: PRAGMA journal_mode=WAL, PRAGMA busy_timeout=5000, CREATE TABLE IF NOT EXISTS invocations
  throw new Error("initDatabase: not yet implemented — Plan 02 fills this in");
}
```

Note: `db.ts` is complete for the wrapper class. Only `initDatabase` is a stub — Plan 02 implements it.

**extension/src/session.ts** — session metadata collection stubs:

```typescript
import * as vscode from "vscode";

export interface SessionMetadata {
  workspace_path: string | null;
  user_data_dir: string | null;
  profile: string | null;
  local_ide_path: string;
  remote_name: string | null;
  remote_server_path: string | null;
  server_commit_hash: string | null;
  local_session_hash: string;
}

export function collectSessionMetadata(
  _context: vscode.ExtensionContext,
): SessionMetadata {
  // Implemented in Plan 03 (Wave 1)
  // Will parse vscode.env.appRoot for commit hash, parse globalStorageUri for profile/user_data_dir (D-01)
  throw new Error(
    "collectSessionMetadata: not yet implemented — Plan 03 fills this in",
  );
}

export function getSessionJsonPath(_metadata: SessionMetadata): string {
  // Implemented in Plan 03 (Wave 1)
  // SSH remote: ~/.vscode-server/bin/{hash}/this-code-session.json (D-05)
  // Local: ~/.this-code/sessions/{hash}.json (D-04)
  throw new Error(
    "getSessionJsonPath: not yet implemented — Plan 03 fills this in",
  );
}
```

**extension/src/storage.ts** — JSON file write and startup scan stubs:

```typescript
import { Database } from "./db";
import { SessionMetadata } from "./session";

export async function writeSessionJson(
  _filePath: string,
  _metadata: SessionMetadata,
): Promise<void> {
  // Implemented in Plan 03 (Wave 1)
  // Will write JSON containing all SessionMetadata fields to the path
  throw new Error(
    "writeSessionJson: not yet implemented — Plan 03 fills this in",
  );
}

export async function scanExistingRemoteSessions(
  _db: Database,
  _binDir?: string,
): Promise<void> {
  // Implemented in Plan 05 (Wave 2)
  // Will scan binDir (default: ~/.vscode-server/bin/) for this-code-session.json files (STOR-04)
  // binDir parameter makes the function testable without touching real home dir
  // Incremental: skips already-indexed remote_server_path values
  // Fire-and-forget from activate() — must not throw uncaught
  throw new Error(
    "scanExistingRemoteSessions: not yet implemented — Plan 05 fills this in",
  );
}
```

**extension/src/config.ts** — configuration reading (complete implementation; simple VS Code API calls):

```typescript
import * as vscode from "vscode";

export type LogLevel = "off" | "info" | "debug";

export function isEnabled(): boolean {
  return vscode.workspace
    .getConfiguration("thisCode")
    .get<boolean>("enable", true);
}

export function getLogLevel(): LogLevel {
  return vscode.workspace
    .getConfiguration("thisCode")
    .get<LogLevel>("logLevel", "info") as LogLevel;
}
```

Note: `config.ts` is the complete implementation. No stub needed — it uses standard VS Code API.

**extension/src/extension.ts** — activate/deactivate entrypoint stub:

```typescript
import * as vscode from "vscode";
import * as os from "os";
import * as path from "path";
import * as fs from "fs/promises";
import { Database, initDatabase } from "./db";
import { collectSessionMetadata, getSessionJsonPath } from "./session";
import { writeSessionJson, scanExistingRemoteSessions } from "./storage";
import { isEnabled } from "./config";

let db: Database | undefined;
let outputChannel: vscode.OutputChannel | undefined;
let currentInvocationId: number | undefined;

export async function activate(
  context: vscode.ExtensionContext,
): Promise<void> {
  outputChannel = vscode.window.createOutputChannel("This Code");
  context.subscriptions.push(outputChannel);

  if (!isEnabled()) {
    outputChannel.appendLine(
      "[info] This Code is disabled via thisCode.enable setting.",
    );
    return;
  }

  try {
    // Steps 1-7 implemented in Plans 02-06 (Waves 1-3)
    const thisCodeDir = path.join(os.homedir(), ".this-code");
    await fs.mkdir(thisCodeDir, { recursive: true });
    await fs.mkdir(path.join(thisCodeDir, "sessions"), { recursive: true });

    const dbPath = path.join(thisCodeDir, "sessions.db");
    db = new Database(dbPath);
    await initDatabase(db);

    const metadata = collectSessionMetadata(context);
    const sessionJsonPath = getSessionJsonPath(metadata);
    await writeSessionJson(sessionJsonPath, metadata);

    const result = await db.run(
      `INSERT INTO invocations
       (workspace_path, user_data_dir, profile, local_ide_path,
        remote_name, remote_server_path, open_files)
       VALUES (?, ?, ?, ?, ?, ?, ?)`,
      [
        metadata.workspace_path,
        metadata.user_data_dir,
        metadata.profile,
        metadata.local_ide_path,
        metadata.remote_name,
        metadata.remote_server_path,
        "[]",
      ],
    );
    currentInvocationId = result.lastID;

    context.subscriptions.push(
      vscode.workspace.onDidOpenTextDocument(() => {
        if (db && currentInvocationId !== undefined) {
          updateOpenFiles(db, currentInvocationId).catch(() => {});
        }
      }),
      vscode.workspace.onDidCloseTextDocument(() => {
        if (db && currentInvocationId !== undefined) {
          updateOpenFiles(db, currentInvocationId).catch(() => {});
        }
      }),
    );

    // Fire-and-forget — do NOT await (STOR-04, Pitfall 6)
    scanExistingRemoteSessions(db).catch((err) => {
      outputChannel?.appendLine(
        `[info] Startup scan error: ${(err as Error).message}`,
      );
    });

    outputChannel.appendLine(
      `[info] This Code activated. Invocation ID: ${currentInvocationId}`,
    );
  } catch (err: unknown) {
    const msg = err instanceof Error ? err.message : String(err);
    outputChannel?.appendLine(`[info] This Code activation failed: ${msg}`);
  }
}

export async function deactivate(): Promise<void> {
  try {
    await db?.close();
  } catch {
    // Best-effort close
  }
}

async function updateOpenFiles(db: Database, rowId: number): Promise<void> {
  // Implemented in Plan 04 (Wave 2)
  // Will rebuild open_files from vscode.workspace.textDocuments (D-02)
  // Filters to uri.scheme === 'file'; handles false positives from language changes
  const openFiles = vscode.workspace.textDocuments
    .filter((doc) => !doc.isClosed && doc.uri.scheme === "file")
    .map((doc) => doc.uri.fsPath);
  try {
    await db.run("UPDATE invocations SET open_files = ? WHERE id = ?", [
      JSON.stringify(openFiles),
      rowId,
    ]);
  } catch {
    // DB error — log and continue
  }
}
```

**extension/src/test/extension.test.ts** — integration test stubs for all 16 Phase 1 requirements:

```typescript
import * as assert from "assert";
import * as path from "path";
import * as os from "os";

// Integration test stubs for Phase 1. Each suite corresponds to a requirement.
// Tests run inside a real VS Code instance via @vscode/test-electron.
// Plans 02-07 fill in assertions as implementation is completed.

suite("EXT-01: Extension ID", () => {
  test("publisher.name is whardier.this-code", () => {
    // Verified by manifest check in CI — no runtime assertion needed
    const pkg = require("../../package.json");
    assert.strictEqual(pkg.publisher + "." + pkg.name, "whardier.this-code");
  });
});

suite("EXT-02: extensionKind workspace", () => {
  test('extensionKind is ["workspace"]', () => {
    const pkg = require("../../package.json");
    assert.deepStrictEqual(pkg.extensionKind, ["workspace"]);
  });
});

suite("EXT-03: Activation event", () => {
  test("activationEvents includes onStartupFinished", () => {
    const pkg = require("../../package.json");
    assert.ok(pkg.activationEvents.includes("onStartupFinished"));
  });
});

suite("EXT-04: No UI contributions", () => {
  test("contributes.commands does not exist", () => {
    const pkg = require("../../package.json");
    assert.strictEqual(pkg.contributes.commands, undefined);
  });
});

suite("EXT-05: Two configuration settings", () => {
  test("exactly thisCode.enable and thisCode.logLevel", () => {
    const pkg = require("../../package.json");
    const keys = Object.keys(pkg.contributes.configuration.properties).sort();
    assert.deepStrictEqual(keys, ["thisCode.enable", "thisCode.logLevel"]);
  });
});

suite("STOR-01: Per-instance JSON", () => {
  test("TODO: JSON file written at correct path after activation", () => {
    // Plan 03 fills this assertion
    assert.ok(true, "stub — implement in Plan 03");
  });
});

suite("STOR-02: SQLite WAL mode", () => {
  test("TODO: sessions.db exists with journal_mode=wal", () => {
    // Plan 02 fills this assertion
    assert.ok(true, "stub — implement in Plan 02");
  });
});

suite("STOR-03: Schema columns", () => {
  test("TODO: invocations table has all required columns", () => {
    // Plan 02 fills this assertion
    assert.ok(true, "stub — implement in Plan 02");
  });
});

suite("STOR-04: Startup scan", () => {
  test("TODO: existing session JSONs indexed on activation", () => {
    // Plan 05 fills this assertion
    assert.ok(true, "stub — implement in Plan 05");
  });
});

suite("STOR-05: ~/.this-code directory creation", () => {
  test("TODO: directory created on first activation", () => {
    // Plan 03 fills this assertion
    assert.ok(true, "stub — implement in Plan 03");
  });
});

suite("TRACK-01: Workspace path", () => {
  test("TODO: workspace_path recorded in SQLite", () => {
    // Plan 03 fills this assertion
    assert.ok(true, "stub — implement in Plan 03");
  });
});

suite("TRACK-02: Commit hash", () => {
  test("TODO: server_commit_hash is 40-char hex or null", () => {
    // Plan 03 fills this assertion
    assert.ok(true, "stub — implement in Plan 03");
  });
});

suite("TRACK-03: user_data_dir and profile", () => {
  test("TODO: user_data_dir non-null; profile null-safe", () => {
    // Plan 03 fills this assertion
    assert.ok(true, "stub — implement in Plan 03");
  });
});

suite("TRACK-04: Open file manifest", () => {
  test("TODO: open_files updates after document event", () => {
    // Plan 04 fills this assertion
    assert.ok(true, "stub — implement in Plan 04");
  });
});

suite("TRACK-05: No save triggers", () => {
  test("onDidSaveTextDocument is not registered", () => {
    // Static check — grep enforces this; test is documentation
    assert.ok(true, "enforced by grep: no onDidSaveTextDocument in src/");
  });
});

suite("PLAT-01: macOS and Linux", () => {
  test("TODO: CI matrix runs on macOS-latest and ubuntu-latest", () => {
    // Plan 07 fills this assertion
    assert.ok(true, "stub — implement in Plan 07");
  });
});
```

The test file uses `require('../../package.json')` which resolves relative to the compiled test output in `dist/` or `out/` — adjust the path if needed based on where @vscode/test-cli compiles the tests. The five EXT-\* tests (manifest checks) can run immediately without a real VS Code host; the STOR/TRACK tests need a real host and will be stubs until Plans 02-06 implement them.
</action>
<verify>
<automated>cd /Users/spencersr/src/github/whardier/this-code && grep -l "export class Database" extension/src/db.ts && grep -l "export interface SessionMetadata" extension/src/session.ts && grep -l "export async function writeSessionJson" extension/src/storage.ts && grep -l "export function isEnabled" extension/src/config.ts && grep -l "export async function activate" extension/src/extension.ts && grep -c "suite(" extension/src/test/extension.test.ts | grep -qE "^1[56]$" && echo "all source stubs present" || (grep -c "suite(" extension/src/test/extension.test.ts; echo "source stubs present") && test ! -f extension/src/test/index.ts && echo "index.ts correctly absent"</automated>
</verify>
<done> - extension/src/db.ts exports Database class with run/get/all/close, and initDatabase stub - extension/src/session.ts exports SessionMetadata interface, collectSessionMetadata stub, getSessionJsonPath stub - extension/src/storage.ts exports writeSessionJson stub and scanExistingRemoteSessions stub (with optional binDir parameter) - extension/src/config.ts exports isEnabled and getLogLevel (complete implementations) - extension/src/extension.ts exports activate and deactivate with full structure (calling into stubs) - extension/src/test/extension.test.ts has 16 test suites (one per requirement) - extension/src/test/index.ts does NOT exist — @vscode/test-cli discovers tests via .vscode-test.js glob - TypeScript compiles with no structural errors: cd extension && npx tsc --noEmit exits 0 (allowing stub throws)
</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                        | Description                                                                                |
| ------------------------------- | ------------------------------------------------------------------------------------------ |
| filesystem → npm install        | npm install downloads packages from registry; package-lock.json pins exact versions        |
| extension source → build output | esbuild bundles; @vscode/sqlite3 excluded from bundle (native binary must ship separately) |

## STRIDE Threat Register

| Threat ID | Category          | Component                           | Disposition | Mitigation Plan                                                                                                                                        |
| --------- | ----------------- | ----------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-01-01   | Tampering         | extension/.vscodeignore             | mitigate    | Include `!node_modules/@vscode/sqlite3/**` — omitting this line causes silent activation failure on VSIX install; verified by acceptance criteria grep |
| T-01-02   | Tampering         | extension/esbuild.js external array | mitigate    | Both 'vscode' and '@vscode/sqlite3' must appear in external; acceptance criteria validates this                                                        |
| T-01-03   | Denial of Service | npm install failure                 | accept      | If npm install fails, executor retries; no security risk, only availability                                                                            |

</threat_model>

<verification>
After both tasks complete:

```bash
# Manifest validation
node -e "const p=require('./extension/package.json');const id=p.publisher+'.'+p.name;console.assert(id==='whardier.this-code');console.assert(JSON.stringify(p.extensionKind)==='[\"workspace\"]');console.assert(p.activationEvents.includes('onStartupFinished'));console.assert(!p.contributes.commands);console.log('manifest OK')"

# sqlite3 installed
test -d extension/node_modules/@vscode/sqlite3 && echo "sqlite3 OK"

# .vscodeignore has preservation line
grep "!node_modules/@vscode/sqlite3" extension/.vscodeignore && echo "vscodeignore OK"

# Source modules exist
ls extension/src/{extension,db,session,storage,config}.ts

# Test stubs exist (no index.ts)
ls extension/src/test/extension.test.ts
test ! -f extension/src/test/index.ts && echo "index.ts correctly absent"

# TypeScript type-check (stubs will pass; throws are valid TS)
cd extension && npx tsc --noEmit && echo "typecheck OK"
```

</verification>

<success_criteria>

- extension/package.json: ID is `whardier.this-code`, extensionKind is `["workspace"]`, activationEvents is `["onStartupFinished"]`, no contributes.commands, exactly two settings (thisCode.enable, thisCode.logLevel)
- extension/node_modules/@vscode/sqlite3/ exists with prebuilt native binary
- extension/.vscodeignore contains `!node_modules/@vscode/sqlite3/**`
- extension/esbuild.js contains `external: ['vscode', '@vscode/sqlite3']`
- All five TypeScript source files exist and export their documented interfaces
- 16 test suites exist in extension/src/test/extension.test.ts
- extension/src/test/index.ts does NOT exist
- `cd extension && npx tsc --noEmit` exits 0
  </success_criteria>

<output>
After completion, create `.planning/phases/01-extension-core-storage-foundation/01-01-SUMMARY.md` documenting:
- All files created (note: index.ts intentionally not created)
- npm install outcome (node_modules size, @vscode/sqlite3 native binary platform)
- Any deviations from this plan with rationale
- TypeScript typecheck result
</output>
