name: CLI Release

on:
  push:
    tags:
      - 'cli/v*'

jobs:
  build-cli:
    name: Build CLI (${{ matrix.target }})
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            target: linux-x64
          - os: ubuntu-24.04-arm
            target: linux-arm64
          - os: macos-latest
            target: darwin-arm64
          - os: macos-latest
            target: darwin-x64
            rust_target: x86_64-apple-darwin
    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.rust_target }}

      - name: Cache Cargo registry and build artifacts
        uses: actions/cache@v6
        with:
          path: |
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            cli/target/
          key: ${{ runner.os }}-${{ matrix.rust_target || 'native' }}-cargo-${{ hashFiles('cli/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-${{ matrix.rust_target || 'native' }}-cargo-

      - name: Build release binary
        working-directory: cli
        run: cargo build --release ${{ matrix.rust_target && format('--target {0}', matrix.rust_target) || '' }}

      - name: Strip binary
        working-directory: cli
        run: strip ${{ matrix.rust_target && format('target/{0}/release/this-code', matrix.rust_target) || 'target/release/this-code' }}

      - name: Rename binary with platform suffix
        working-directory: cli
        run: cp ${{ matrix.rust_target && format('target/{0}/release/this-code', matrix.rust_target) || 'target/release/this-code' }} this-code-${{ matrix.target }}

      - name: Upload CLI binary artifact
        uses: actions/upload-artifact@v6
        with:
          name: cli-${{ matrix.target }}
          path: cli/this-code-${{ matrix.target }}

  release-cli:
    name: Create CLI GitHub Release
    needs: [build-cli]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Download all CLI binary artifacts
        uses: actions/download-artifact@v6
        with:
          pattern: cli-*
          merge-multiple: true
          path: dist/

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: dist/this-code-*
          name: CLI ${{ github.ref_name }}
